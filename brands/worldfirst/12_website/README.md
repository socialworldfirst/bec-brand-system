# 12 Website

Stub in this system. The web design language is indexed at `~/Documents/Claude/WFxWD_DLS/` (BEC Figma DLS + staging build captures); `/wf_dls` builds from it, `/wf_site` matches production worldfirst.com.
